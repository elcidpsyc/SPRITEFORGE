# SPRITEFORGE → Godot 4.x

Este esqueleto vive no pack exportado. Use **Batch Export** ou **Pack Godot 4**
na aba Guerreiros para baixar o ZIP completo (`atlas.json` + PNGs + importer).

Drop-in: copie `<id>/` para `res://sprites/spriteforge/<id>/` e rode
`engines/godot/spriteforge_importer.gd` (File → Run). Filtro Point / nearest.
